this is the server that consist of routes s and handle requests coming from the front end
get your api and put in .env file that you will create
and you also need to downlad the modules or follow the given command:

  go to backend repo and type cmd in repository search-bar
  then type npm init -y in cmd
  then type npm install express dotenv cors axios

and your backend server is ready
